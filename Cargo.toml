[package]
name = "nanopython"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "A minimal Rust-based Python interpreter"

[[bin]]
name = "nanopy"
path = "src/main.rs"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
